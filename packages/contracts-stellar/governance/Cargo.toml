[package]
name = "governance"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]
soroban-sdk = "21.4"

[dev-dependencies]
soroban-sdk = { version = "21.4", features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils"]