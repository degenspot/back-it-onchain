[workspace]
resolver = "2"

members = [
    "call_registry",
    "outcome_manager",
      "governance"
]

[dev-dependencies]
proptest = "1.4.0"
soroban-sdk = { version = "20.0.0", features = ["testutils"] }